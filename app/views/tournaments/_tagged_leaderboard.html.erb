<%# Locals: tournament, leaderboard, viewer_scope %>
<%
  # Resolve winner identity once so the row loop can highlight by entry id
  # without issuing an EXISTS-per-row, and the angler-name cell uses the
  # entry's display_name (matches the standard leaderboard partial).
  winner_placement = tournament.drawn_winning_placement if tournament.drawn_at.present?
  winner_catch     = winner_placement&.catch
  winning_entry_id = winner_placement&.tournament_entry_id
  # Tournament#drawn_winner_voided? / #tickets_remain? hold the rules (shared
  # with the admin edit page); tickets_remain only matters once the draw is void.
  # Guarded on the winner resolved above: a recorded winner with no draw time
  # is no winner here, so it can't be a void one either.
  winner_void    = winner_placement.present? && tournament.drawn_winner_voided?
  tickets_remain = winner_void && tournament.tickets_remain?
  can_draw    = current_user&.organizer_in?(tournament.club) && tournament.ended?
%>
<div id="leaderboard">
  <% if winner_void %>
    <div class="bg-rose-900/30 border border-rose-700/40 rounded-lg p-4 mb-4">
      <p class="text-sm text-rose-200 uppercase tracking-wide">Draw void</p>
      <p class="text-rose-100 mt-1">
        The drawn fish (<%= winner_catch.user.name %>, tag <%= winner_catch.tag_number %>)
        no longer holds a ticket.
        <% if tickets_remain %>
          An organizer needs to re-draw.
        <% else %>
          No other tickets remain, so there is nothing to re-draw.
        <% end %>
      </p>
    </div>
    <% if can_draw && tickets_remain %>
      <%= button_to "Re-draw winner", draw_organizers_tournament_path(tournament, force: 1),
                    method: :post, data: { turbo_confirm: "Re-roll the draw among the remaining tickets?" },
                    class: "mb-4 inline-flex h-10 px-4 items-center justify-center rounded-lg bg-amber-600 hover:bg-amber-500 text-white font-semibold" %>
    <% end %>
  <% elsif winner_catch %>
    <div class="bg-amber-900/30 border border-amber-700/40 rounded-lg p-4 mb-4">
      <p class="text-sm text-amber-200 uppercase tracking-wide">Winner</p>
      <p class="text-xl text-amber-100 font-semibold mt-1">
        <%= winner_catch.user.name %> &mdash; tag
        <% winner_url, winner_frame = catch_link_target(tournament: tournament, catch_id: winner_catch.id) %>
        <% if winner_url %>
          <%= link_to winner_catch.tag_number, winner_url,
                      data: ({ turbo_frame: winner_frame } if winner_frame),
                      class: "underline underline-offset-2 hover:text-amber-50" %>
        <% else %>
          <%= winner_catch.tag_number %>
        <% end %>
      </p>
      <p class="text-xs text-amber-200/80 mt-1">
        Caught <%= l(winner_catch.captured_at_device, format: :short) %> &middot;
        drawn <%= l(tournament.drawn_at, format: :short) %>
      </p>
    </div>
  <% elsif tournament.ended? %>
    <div class="bg-slate-800 border border-slate-700 rounded-lg p-3 mb-4 text-sm text-slate-300">
      Tournament ended — awaiting draw.
    </div>
  <% end %>

  <% if can_draw && tournament.drawn_at.nil? %>
    <%= button_to "Draw winner", draw_organizers_tournament_path(tournament),
                  method: :post, data: { turbo_confirm: "Run the draw now?" },
                  class: "mb-4 inline-flex h-10 px-4 items-center justify-center rounded-lg bg-amber-600 hover:bg-amber-500 text-white font-semibold" %>
  <% end %>

  <table class="w-full text-sm">
    <thead class="text-slate-400 border-b border-slate-700">
      <tr>
        <th class="text-left py-2 w-10">#</th>
        <th class="text-left py-2">Angler</th>
        <th class="text-right py-2 w-20">Tickets</th>
        <th class="text-left py-2 pl-4">Tags</th>
      </tr>
    </thead>
    <tbody>
      <% leaderboard.each_with_index do |row, i| %>
        <% is_winner = winning_entry_id && row[:entry].id == winning_entry_id %>
        <tr class="<%= 'bg-amber-900/20' if is_winner %> border-b border-slate-800">
          <td class="py-2"><%= i + 1 %></td>
          <td class="py-2">
            <%= row[:entry].display_name %>
            <% if (roster = team_roster_line(tournament, row[:entry])) %>
              <div class="text-xs text-slate-500 mt-1"><%= roster %></div>
            <% end %>
          </td>
          <td class="py-2 text-right font-mono"><%= row[:total] %></td>
          <td class="py-2 pl-4 text-slate-400 text-xs">
            <% links = row[:fish].map do |f|
                 if f[:tag_number].blank?
                   "?"
                 else
                   url, frame = catch_link_target(tournament: tournament, catch_id: f[:id])
                   if url
                     link_to(f[:tag_number], url,
                             data: ({ turbo_frame: frame } if frame),
                             class: "text-blue-300 underline-offset-2 hover:underline")
                   else
                     f[:tag_number]
                   end
                 end
               end %>
            <%= safe_join(links, ", ") %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
